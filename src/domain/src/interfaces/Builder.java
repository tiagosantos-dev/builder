package interfaces;

public interface Builder {
    Builder carne(String carne);
    Builder pao(String pao);
    Builder verduras(String verdura);
    Builder queijo(String queijo);
    Builder maionese(Boolean maionese);
  
} 